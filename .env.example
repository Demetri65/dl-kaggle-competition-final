# Keep the real .env local only.
# In VSCode Colab, use the notebook upload widget to save this local .env to /content/.env.
# Generate the token from Kaggle Settings > API > Generate New Token.
KAGGLE_API_TOKEN=your_kaggle_api_token_here
