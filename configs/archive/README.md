# Archived Experiment Configs

These configs are preserved for historical context but are intentionally outside
`configs/experiments` so they are not discoverable as active experiment
overrides.

Current active Stage 0 formulations:
- `f04_candidate_yes_no` as the selected formulation
- `f03_multimodal_letter` as the restricted-output comparator

Current active Stage 1 eval-only screening ablations:
- `x11_f04_candidate_choices`
- `x12_f04_candidate_hint`
